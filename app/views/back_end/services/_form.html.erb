

<%= form_for(@back_end_service) do |f| %>
    <% if @back_end_service.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@back_end_service.errors.count, "error") %> prohibited this back_end_service from being saved:</h2>

          <ul>
            <% @back_end_service.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :name, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :name, class:"form-control" %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :identifier, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_field :identifier, class:"form-control" %>
      </div>
    </div>

    <div class="form-group">
      <div class="col-md-2">
      </div>
      <div class="col-sm-10">
        <%= f.check_box :active%>
        <%= f.label :active %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :header, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_area :header, style: "width: 100%;" %>
      </div>
    </div>

    <div class="form-group">
      <%= f.label :body, class: "col-sm-2 control-label" %>
      <div class="col-sm-10">
        <%= f.text_area :body, :class => 'ckeditor' %>
      </div>
    </div>


    <div class="row">
      <div class="pull-right col-md-2">
        <%= submit_tag  "Submit", class: 'btn btn-primary' %>
      </div>
    </div>
<% end %>
