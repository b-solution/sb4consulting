

<%= form_for(@back_end_page) do |f| %>
    <% if @back_end_page.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@back_end_page.errors.count, "error") %> prohibited this back_end_page from being saved:</h2>

          <ul>
            <% @back_end_page.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="form-group">
      <div class="col-md-2">
      </div>
      <div class="col-sm-10">
        <%= f.label :locale %>
        <%= f.select :locale, options_for_select(['fr','en'], @back_end_page.locale)%>

      </div>
    </div>

    <div class="form-group">
      <div class="col-sm-12">
        <%= f.text_area :body, :class => 'ckeditor' %>
      </div>
    </div>


    <div class="row">
      <div class="pull-right col-md-2">
        <%= submit_tag  "Submit", class: 'btn btn-primary' %>
      </div>
    </div>
<% end %>
